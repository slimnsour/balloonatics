// Checks player collision, taking into account types of blocks
// argument0 and 1 are future x and y positions
var collide = instance_place(argument0,argument1,argument2);
blocked = true;
if (collide != noone) {
    
}

return (collide != noone && blocked)
